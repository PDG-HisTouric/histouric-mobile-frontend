import 'package:flutter/material.dart';

class RoutesTempScreen extends StatelessWidget {
  const RoutesTempScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rutas'),
      ),
      body: const Center(
        child: Text('RoutesTempScreen'),
      ),
    );
  }
}
